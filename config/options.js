// Default init options
var options = {
	formats: ["text"], // the input/output types expected to be used - options (TBA): text, audio...
	data: null, // provided data, when intellect is in "active" mode
	interpreter: false, // third-party method to relate tags to responses
}

module.exports = options;
